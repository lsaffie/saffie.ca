.post-container.rounded
  #title
    =@post.title
  .date
    =@post.created_at.strftime("%b %d, %Y")
  #post_body
    =RedCloth.new(@post.body).to_html.html_safe


  %p
    %hr
    #comments
      =render :partial => 'comments/index.html.haml'
      =render :partial => 'comments/comment.html.haml'
